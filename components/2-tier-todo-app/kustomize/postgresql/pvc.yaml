apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todo-postgresql
  labels:
    app.kubernetes.io/name: todo-postgresql
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
